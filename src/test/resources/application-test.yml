spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/workpool_db_test
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: localhost
    port: 3025
    username: test
    password: test

app:
  jwt:
    secret: workpool-test-secret-key-minimum-32-chars
    expiration: 86400000
  frontend:
    url: http://localhost:5173